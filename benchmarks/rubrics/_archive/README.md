# Archived rubrics

These were written before the 6-category set was locked in [`CLAUDE.md` §6](../../../CLAUDE.md#6-the-6-categories-final). Per locked decision **L5** ("6 broad categories — fewer = fewer misclassifications"), they are not part of the active rubric set.

Kept here so the work isn't lost — patterns and check ideas may be useful when refining the active rubrics.

## Files

- `open_ended_generation.json` — was a 7th category for ideation/brainstorm prompts. The active set folds those into `task_only` (when count + format are constrained) or `vague_instruction` (when they aren't).
